package com.roko.kronos.constants

import androidx.compose.foundation.layout.padding
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

const val DEFAULT_PRECISION_DROP = 3 // 3 - preserve from the second above, 2 - preserve from the tenth of a second above
const val DEFAULT_NOTIFICATIONS_ENABLED = false

val MODIFIER_PADDING_BOTTOM_STANDARD: Modifier = Modifier.padding(bottom = 20.dp)
val MODIFIER_PADDING_BOTTOM_EXTRA: Modifier = Modifier.padding(bottom = 60.dp)
